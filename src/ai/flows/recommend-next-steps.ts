// src/ai/flows/recommend-next-steps.ts
// Temporary stub; replace with real logic later.
export type Recommendation = { action: string; reason?: string };

export async function recommendNextSteps(_: unknown): Promise<Recommendation[]> {
  return [];
}

export default recommendNextSteps;
